version: "3.7"

services:
    hello-container:
        image: gentux/alpine-bash:ericabwalker
        volumes:  
            - ./scripts:/home/scripts
        command: /home/scripts/welcome-compose.sh

    data-container:
        image: alpine:latest 
        volumes: 
            - ./scripts:/home/scripts